{
  "name": "back-to-the-future-v2",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ 0xszeth/back-to-the-future-v2",
    "create-local": "graph create --node http://localhost:8020/ 0xszeth/back-to-the-future-v2",
    "remove-local": "graph remove --node http://localhost:8020/ 0xszeth/back-to-the-future-v2",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 0xszeth/back-to-the-future-v2"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
